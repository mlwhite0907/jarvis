Wire Home Assistant Assist to use the already-running Hermes brain shim + Wyoming voice engines so Michael can talk to it. Work headless over SSH: ssh -o ConnectTimeout=15 -o BatchMode=yes mlwhite0907@100.91.48.99 "<cmd>".

ENVIRONMENT (already running, do NOT rebuild): HA in Docker container "homeassistant" (port 8123); HA config dir = host path /home/mlwhite0907/homeassistant mounted to /config in the container. HA is onboarded; admin bearer + refresh token in /home/mlwhite0907/ha_token.txt. Already up: wyoming-whisper (127.0.0.1:10300), wyoming-piper (127.0.0.1:10200), hermes-openai-shim (127.0.0.1:8642, OpenAI-compatible, model id "hermes-agent").

GOAL: a working Assist pipeline "Jarvis" = Whisper STT -> Hermes (via shim) -> Piper TTS, set as preferred, verified by the HA conversation API returning a Hermes-generated reply.

STEPS (headless; prefer HA REST/config-flow APIs with the admin token; fall back to editing /config/.storage entries + container restart, backing up first):
1) Install extended_openai_conversation WITHOUT HACS: git clone https://github.com/jekalmin/extended_openai_conversation /tmp/eoc ; copy its custom_components/extended_openai_conversation folder to /home/mlwhite0907/homeassistant/custom_components/extended_openai_conversation ; restart the homeassistant container; confirm HA logs show it loaded.
2) Configure the Wyoming integration for whisper (127.0.0.1:10300) and piper (127.0.0.1:10200); confirm an stt.* and tts.* entity appear.
3) Configure Extended OpenAI Conversation: base_url=http://127.0.0.1:8642/v1 , api_key=local , chat model=hermes-agent , temperature 0.3. Confirm a conversation.* entity appears.
4) Create an Assist pipeline "Jarvis": conversation_engine = the extended_openai conversation entity, stt_engine = wyoming whisper, tts_engine = wyoming piper, language en; set it as preferred.
5) VERIFY: POST http://localhost:8123/api/conversation/process with {"text":"Say hello in five words","agent_id":"<conversation entity id>"} and the admin bearer token; confirm the reply text came back (generated by Hermes via the shim). Confirm stt/tts entities exist.

GUARDRAILS: ADDITIVE ONLY. Only touch the homeassistant container and its /config. Do NOT modify/stop/restart hermes-gateway, pihole, the wyoming containers, or the shim. Back up any /config/.storage/* file before editing. If a step truly needs a GUI click impossible via API/storage, do everything else and state EXACTLY which single click Michael must do.

Write a concise report to /home/mlwhite0907/jarvis_phase4_report.txt AND print it: each step done/failed, entity IDs created, the conversation API verification output (actual reply text), and anything left for Michael.
